#ifndef IDS_H
#define IDS_H

#include <wx/wx.h>

enum
{
 	wa_nothing_event = wxID_HIGHEST + 1,
 	wa_notebook,
 	vigenere_calc_key,
 	vigenere_keys,
 	vigenere_current_key,
 	vigenere_txt,
	affine_txt,
	frequency_txt,
	contact_txt,
	contact_asym,
	subst_txt,
	subst_grid,
	subst_select
};

#endif
